#include "moving_goal.h"
#include <iostream>

// Return a voltage to move the turret to a non-static goal
Voltage TurretController::Update(TurretState state, Time dt) {
  std::cout << "Implement the control function here! " __FILE__ << std::endl;
  return 0 * V;
}
