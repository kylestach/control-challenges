#include "simple.h"
#include <iostream>

// Return a force to move the block to goal_, even with the spring's force
Voltage FlywheelController::Update(AngularVelocity velocity, Time dt) {
  std::cout << "Implement the control function here! " __FILE__ << std::endl;
  return 0 * V;
}
