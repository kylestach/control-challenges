#include "simple.h"
#include <iostream>

// Return a force to move the block to goal_, even with the spring's force
Voltage ElevatorController::Update(ElevatorState current_state, Time dt) {
  std::cout << "Implement the control function here! "
               "(solutions/elevator/simple.cpp)"
            << std::endl;
  return 0 * V;
}
