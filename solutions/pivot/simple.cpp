#include "simple.h"
#include <iostream>

// Return a force to move the pivot to 50 degrees
Force PivotController::Update(PivotState state, Time dt) {
  std::cout << "Implement the control function here! " __FILE__ << std::endl;
  return 0 * N;
}
